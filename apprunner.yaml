version: 1.0
runtime: nodejs18
build:
  commands:
    build:
      - npm install
      - npm run build
run:
  runtime-version: 18
  command: npm run start:prod
  network:
    port: 8080
    env: PORT
  env:
    - name: NODE_ENV
      value: production
